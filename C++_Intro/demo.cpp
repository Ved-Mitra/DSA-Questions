//#include<iostream>
//basic input/output library

//to include all the libraries we use
#include<bits/stdc++.h>

using namespace std; //the program uses std every time to call all fuctions


int main()
{
    // without use of    using namespace std;
    /*
    std::cout << "Hey Ved!!" << "\n";
    std::cout <<"Hey Ved!!!" << std::endl;
    // endl and \n are same but \n is preffered as it is faster
    std::cout <<"Hey Ved!!!" << "Hey Kanha!!" << std::endl << "Hey both";
    */




    //with use of   using namespace std;
    /*
    cout << "Hey Ved!!" << "\n";
    cout <<"Hey Ved!!!" << endl;
    // endl and \n are same but \n is preffered as it is faster
    std::cout <<"Hey Ved!!!" << "Hey Kanha!!" << std::endl << "Hey both"; //can still use std to specify
    */




    //Inputs
    /*
    int x;
    cin >> x;
    cout << "Value of x: " << x;

    int a,b;
    cin >> a >> b; //input can be given in same line or different lines
    cout << "Value of a: " << a << " and b: " << b;
    
    string s,s2;
    cin>>s; // stores inly till the white space
    cout<<"s:" << s << endl;
    getline(cin,s2); // store till the \n
    cout<<"s2: " << s2;
    
    char ch;
    cin>>ch;
    cout<<ch;

    string s3="VED MITRA VERMA"
    cout<<s3[2]; //would give D
    int len=s3.size(); //to get the length of string
    */



    //if-else is same as C language
    //switch case is same as Java



    //array
    /*
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    single and double array is same
    */



    //loops same as C language
    //functions same as C language
    
    return 0;
}