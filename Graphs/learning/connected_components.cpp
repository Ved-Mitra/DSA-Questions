#include <bits/stdc++.h>
using namespace std;

//suppose graph is broken into non connected components then we use a vivited array 
//vector<bool> arr(n+1,false); n--number of nodes
//then we mark a node true in vivited if visited then we go to each node and check whether vivsted or not

int main(){

}