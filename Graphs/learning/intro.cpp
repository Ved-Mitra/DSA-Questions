#include <bits/stdc++.h>
using namespace std;

// CYCLE - start and end at the same node
// undirected cyclic graph (has a cycle)
// directed acyclic graph (DAG) (no cycle)

// PATH - contain a lot of node but each are reachable (node cannot appear twice in path)

// DEGREE - number of edges that go or come out
// total degree of a graph = 2* number of edges in a graph
// Directed graph -> (Indegree, Outdegree)

// Edge weight - every edge has a weight
// by default assume unit weight



int main()
{
    
}